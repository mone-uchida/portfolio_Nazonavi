<div class="event-box">
  <div class="event-info">
    <h3><%= link_to event.name,event_path(event) %></h3>
      <p>チケット: 一人当たり<%= event.price %>円程度</p>
      <p>会場: <%= link_to event.place, spot_path(event.spot_id) %></p>
      <% if event.start_at.present? %>
        <p>イベント開始日: <%= event.start_at.strftime('%Y年%m月%d日') %></p>
      <% else %>
        <p>イベント開始日: - </p>
      <% end %>
      <% if event.finish_at.present? %>
        <p>イベント終了日: <%= event.finish_at.strftime('%Y年%m月%d日') %></p>
      <% else %>
        <p>イベント終了日: - </p>
      <% end %>
  </div>
  <img class="event-image" src="https://<%= Rails.application.credentials.aws[:s3_URL] %>/titles/event_image/<%= event.image %>" >
</div>
